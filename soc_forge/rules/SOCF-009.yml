version: 1
rules:
- id: SOCF-009
  enabled: true
  title: Audit logs cleared
  severity: high
  score: 80
  mitre:
  - tactic: Defense Evasion
    technique: Indicator Removal on Host
    id: T1070
  match:
    all:
    - field: event_id
      op: eq
      value: 1102
  emit:
    details:
      host: ${host}
      ip: ${ip}
      actor: ${actor}
      message: ${message}
      suspicious: true
  description: Detects Windows audit log clearing, which can indicate attempted evidence
    removal.
  logsource: windows-security
  tags:
  - windows
  - defense-evasion
  - log-clearing
  author: SOC-Forge
  created: '2026-07-17'
